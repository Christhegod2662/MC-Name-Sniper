MC-Sniper Webhook addition

NOTE: do "pip install -r  requirements.txt" in the same directory as the requirements.txt file!

Before running "python main.py" you must input a webhook into the config.json or it'll print an error!
Default delay is 0.7 plus a api delay of 0.3!
Some names might be open in another day, so its just telling you its open and its not calimed!

In that case check the "date available" link!